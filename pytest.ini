[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    integration: tests that hit live council websites (may be flaky)
    unit: fast unit tests that don't require external services
